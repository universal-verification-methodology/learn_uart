# Module 02 examples — Spec → RTL checklist

Track A (UART RTL / TB literacy). Browser lab is shipped.

## Prompts

1. Restate the core idea of **Spec → RTL checklist** in one sentence (ports → blocks → skeleton).
2. From the UART TX spec, list ports, parameters, and three named blocks.
3. Optional: peek at  (archive; prefer **learn_uart** / **learn_spi** / **learn_i2c**) for a UART RTL/TB sketch.

## Stretch

Redo the same flow in `spec-to-rtl` for the SPI or I²C preset.
